- Porque precisamos ter eng/script.pl e um wrapper eng/script.ps1 que chama eng/script.pl? Usar como padrão `perl eng/script.pl` não parece uma boa ideia já que temos o Perl garantido sempre?
- Mojo::Pg::Migrations 5.0 já tem um padrão que usa um diretório de migração ao invés de um único arquivo. (from_dir). Vale revisar se estamos usando as últimas versões das bibliotecas Mojolicious.
- Vale usar `use Modern::Perl '2025';` por padrão nos scripts? (https://metacpan.org/pod/Modern::Perl). Talvez usar `use 5.042` com `use warnings` e `use strict` ou `use autodie` ou ainda `use utf8`. Tivemos problemas com codificação de caracteres, e precisamos avaliar se usar utf8 como padrão e definir um padrão de cabeçalho mínimo para qualquer script perl é uma boa opção, pois atende ao que fazemos na maioria das aplicações que queremos abordar, e quando alguém quiser diferente seja explícito no script perl em questão, mas o padrão ser bem definido é bom pra nós. Então qual a melhor opção? Usar um `use Modern::Perl x`, ou um conjunto bem definido de cabeçalhos com pragmas?
- Qual repositório oficial do berrybrew? https://github.com/dnmfarrell/berrybrew ou https://github.com/stevieb9/berrybrew/?. Precisamos documentar melhor a instalação no Windows. Li em algum lugar que parece que há uma questão histórica de que https://github.com/dnmfarrell/berrybrew nasceu primeiro depois o mantenedor mudou e reescreveu tudo em https://github.com/stevieb9 e este se tornou o oficial. E que a instalação é recomendada via clonar o repositório e executar um script de configuração primeiro. Então precisamos definir qual é o repositório válido e instruir corretamente usuários Windows a fazer a instalação eficazmente.
- cpanfile tem questões de versões parciais? (>=, ~=, etc.) Por exemplo `requires 'Mojolicious', '9.0';` baixa `Mojolicious 9.46` ou exatamente `Mojolicious 9.0`? (https://metacpan.org/dist/Mojolicious). E `requires 'Minion';` baixa qual versão?
- Usar Mojolicious::Plugin::OpenAPI só, ou usar outro como Mojolicious::Plugin::OIDC, ou Mojolicious::Plugin::OAuth2? Ou usá-los em conjunto? Qual a melhor opção para usar cok Keycloak
- Nós estamos fazendo testes que na maioria (se não todos) acessam o banco de dados. Então são testes de integração. Quero ter testes unitários puros para validar as regras de negócio, se possível com mocks para simular os serviços dependentes. É possível? Ainda que tenhamos que modificar como algumas coisas são implementadas.